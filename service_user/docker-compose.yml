version: "3.9"
services:
  service_user:
    image: service_user
    ports:
      - "8888:8888"
    environment:
      - DOCUMENTDB_DATABASE_URI=mongodb://root:example@test_local_mongo_1:27017/

networks:
  default:
    name: ticketoverflow
    external: true
